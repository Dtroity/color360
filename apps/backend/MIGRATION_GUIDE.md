# Инструкция по миграции каталога

Это руководство описывает процесс миграции данных из HTML копии сайта в новую базу данных PostgreSQL.

## Предварительные требования

1. **База данных PostgreSQL** должна быть создана и доступна
2. **Переменные окружения** настроены в `.env`:
   ```
   DATABASE_HOST=localhost
   DATABASE_PORT=5432
   DATABASE_USER=postgres
   DATABASE_PASSWORD=postgres
   DATABASE_NAME=video_shop
   ```
3. **Исходная папка** с HTML файлами должна существовать:
   ```
   C:\Users\Detroyti\Documents\GitHub\hiwatch_site\hiwatch_site_copy
   ```

## Шаг 1: Извлечение данных из HTML

Запустите скрипт для парсинга HTML файлов и извлечения данных:

```bash
cd apps/backend
npx ts-node src/scripts/extract-from-html-clone.ts
```

**Что делает скрипт:**
- Рекурсивно сканирует папку с HTML файлами
- Извлекает данные о товарах (название, цена, описание, характеристики)
- Извлекает категории
- Сохраняет результаты в JSON файлы:
  - `apps/backend/data/extracted/products.json`
  - `apps/backend/data/extracted/categories.json`

**Ожидаемый результат:**
```
🔍 Поиск HTML файлов...
✅ Найдено 25 HTML файлов

📄 Извлечение данных из HTML файлов...

[1/25] ✅ IP-камера HiWatch DS-I200C
[2/25] ✅ Видеорегистратор HiWatch DS-7204HGHI-F1
...

📊 Статистика:
✅ Успешно извлечено: 23 товаров
⚠️  Пропущено: 2 файлов
📁 Категорий: 5
```

## Шаг 2: Копирование изображений

После извлечения данных скопируйте изображения товаров:

```bash
npx ts-node src/scripts/copy-images-from-clone.ts
```

**Что делает скрипт:**
- Находит изображения по URL из JSON файлов
- Копирует их в `apps/backend/uploads/products/`
- Оптимизирует и конвертирует в WebP формат
- Обновляет пути в `products.json`

**Ожидаемый результат:**
```
📖 Чтение products.json...
✅ Загружено 23 товаров

🖼️  Копирование изображений...

✅ ip-kamera-hiwatch-ds-i200c-0.webp
✅ videoregistrator-hiwatch-ds-7204hghi-f1-0.webp
...

📊 Статистика:
✅ Успешно скопировано: 20 изображений
⚠️  Ошибок: 3
```

## Шаг 3: Импорт в базу данных

Импортируйте извлечённые данные в PostgreSQL:

```bash
npx ts-node src/scripts/import-extracted-data.ts
```

**Что делает скрипт:**
- Подключается к PostgreSQL с кодировкой UTF-8
- Создаёт категории (если не существуют)
- Создаёт товары со связями с категориями
- Создаёт записи изображений
- Пропускает дубликаты (по slug)

**Ожидаемый результат:**
```
🔌 Подключение к базе данных...
✅ Подключение установлено

📖 Чтение JSON файлов...
✅ Загружено 23 товаров и 5 категорий

📁 Создание категорий...
  ✅ Создана категория: IP-камеры
  ✅ Создана категория: Видеорегистраторы
  ...

📦 Создание товаров...
[1-10/23] ✅ IP-камера HiWatch DS-I200C
[11-20/23] ✅ Видеорегистратор HiWatch DS-7204HGHI-F1
...

📊 Статистика импорта:
✅ Товаров создано: 23
⏭️  Товаров пропущено: 0
❌ Ошибок: 0

📈 Итого в базе данных:
   Товаров: 23
   Категорий: 5

✅ Импорт завершён!
```

## Проверка результата

Проверьте импортированные данные в базе:

```bash
psql -U postgres -d video_shop
```

```sql
-- Количество товаров
SELECT COUNT(*) FROM products;

-- Количество категорий
SELECT COUNT(*) FROM categories;

-- Примеры товаров (проверка кодировки)
SELECT id, name, price, slug FROM products LIMIT 5;

-- Товары с категориями
SELECT p.name, c.name as category_name 
FROM products p 
LEFT JOIN categories c ON p.categoryId = c.id 
LIMIT 10;

-- Товары с изображениями
SELECT p.name, pi.url 
FROM products p 
LEFT JOIN product_images pi ON pi.productId = p.id 
LIMIT 10;
```

## Устранение проблем

### Ошибка: "HTML файлы не найдены"
- Проверьте путь к исходной папке в скрипте
- Убедитесь, что папка существует и содержит HTML файлы

### Ошибка: "Ошибка подключения к БД"
- Проверьте переменные окружения в `.env`
- Убедитесь, что PostgreSQL запущен
- Проверьте права доступа пользователя

### Ошибка: "Кодировка неверная (иероглифы)"
- Убедитесь, что в DataSource указано `client_encoding: 'UTF8'`
- Проверьте кодировку базы данных: `SHOW server_encoding;`
- Убедитесь, что JSON файлы сохранены в UTF-8

### Товары не извлекаются
- Проверьте структуру HTML файлов
- Адаптируйте селекторы в функции `extractFromHtml()`
- Проверьте логи скрипта для выявления проблемных файлов

### Изображения не копируются
- Проверьте пути к изображениям в JSON
- Убедитесь, что исходные файлы существуют
- Проверьте права на запись в `apps/backend/uploads/products/`

## Повторный импорт

Если нужно переимпортировать данные:

1. **Очистить базу** (осторожно!):
   ```sql
   DELETE FROM product_images;
   DELETE FROM products;
   DELETE FROM categories;
   ```

2. **Запустить импорт заново**:
   ```bash
   npx ts-node src/scripts/import-extracted-data.ts
   ```

## Дополнительные возможности

### Пакетный импорт больших объёмов

Скрипт автоматически использует транзакции для batch операций (по 10 товаров). Для изменения размера batch измените переменную `batchSize` в скрипте.

### Оптимизация изображений

Скрипт `copy-images-from-clone.ts` автоматически:
- Конвертирует изображения в WebP
- Изменяет размер до 800x800px (сохраняя пропорции)
- Устанавливает качество 85%

Для отключения оптимизации закомментируйте блок с `sharp` в скрипте.

## Поддержка

При возникновении проблем:
1. Проверьте логи скриптов
2. Убедитесь, что все зависимости установлены
3. Проверьте версии Node.js и TypeScript
4. Проверьте структуру базы данных (миграции применены)

